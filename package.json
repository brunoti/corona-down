{
  "name": "corona-down",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "android": "yarn workspace mobile android",
    "ios": "yarn workspace mobile ios",
    "studio": "yarn workspace mobile studio",
    "xcode": "yarn workspace mobile xcode",
    "lint": "yarn workspaces run lint",
    "start:prod": "yarn workspace backend start:prod",
    "start:dev": "yarn workspace backend start:dev",
    "seed:config": "yarn workspace backend seed:config",
    "seed:run": "yarn workspace backend seed:run",
    "schema:drop": "yarn workspace backend schema:drop",
    "schema:sync": "yarn workspace backend schema:sync"
  },
  "nohoist": [
    "mobile/react-native"
   ],
  "dependencies": {
    "husky": "^4.2.5",
    "lint-staged": "^10.1.3",
    "react-native": "*"
  },
  "devDependencies": {
    "typescript": "3.7.5"
  }
}
